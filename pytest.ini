[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    unit: Unit tests (no external dependencies)
    integration: Integration tests (requires running API + Neo4j + Ollama)
    eval: Evaluation tests (requires running API with ingested data)
log_cli = true
log_cli_level = INFO
